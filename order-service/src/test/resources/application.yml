spring:
  datasource:
    url: jdbc:h2:mem:order-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate.ddl-auto: create-drop
  kafka:
    bootstrap-servers: localhost:19092
    consumer.group-id: order-test
  cache.type: none
logistics:
  outbox:
    fixed-delay: 3600000
